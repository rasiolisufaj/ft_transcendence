diff --git a/.env.example b/.env.example
index 9a6ca21..6a05de9 100644
--- a/.env.example
+++ b/.env.example
@@ -52,4 +52,4 @@ DATABASE_URL="postgresql://mespapiers:change_me_locally@localhost:5432/mespapier
 # SMTP_PORT=1025
 
 # --- App ---------------------------------------------------------------------
-# APP_URL=https://mespapiers.local
+APP_URL=https://mespapiers.local
diff --git a/docker-compose.yml b/docker-compose.yml
index b1958e7..20e7118 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -1,10 +1,27 @@
 services:
+  # ── NGINX reverse proxy (TLS termination) ──────────────────────────────────
+  nginx:
+    image: nginx:alpine
+    container_name: mespapiers_nginx
+    restart: unless-stopped
+    ports:
+      - "443:443"
+      - "80:80"
+    volumes:
+      - ./infra/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
+      - ./infra/nginx/entrypoint.sh:/entrypoint.sh:ro
+      - nginx-certs:/etc/nginx/certs
+    entrypoint: ["/entrypoint.sh"]
+    depends_on:
+      - web
+
+  # ── Next.js application ────────────────────────────────────────────────────
   web:
     build: .
     container_name: mespapiers_web
     restart: unless-stopped
-    ports:
-      - "3000:3000"
+    expose:
+      - "3000"
     environment:
       DATABASE_URL: "postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}?schema=public"
     depends_on:
@@ -15,6 +32,7 @@ services:
       - ./public:/app/public
       - ./prisma:/app/prisma
 
+  # ── PostgreSQL database ────────────────────────────────────────────────────
   db:
     image: postgres:17-alpine
     container_name: mespapiers_db
@@ -35,3 +53,4 @@ services:
 
 volumes:
   postgres-data:
+  nginx-certs:
diff --git a/infra/nginx/entrypoint.sh b/infra/nginx/entrypoint.sh
new file mode 100755
index 0000000..dc35e4b
--- /dev/null
+++ b/infra/nginx/entrypoint.sh
@@ -0,0 +1,33 @@
+#!/bin/sh
+# ─────────────────────────────────────────────────────────────────────────────
+# MesPapiers — NGINX entrypoint
+#
+# If mkcert certificates exist in /etc/nginx/certs/, use them (green padlock).
+# Otherwise, generate self-signed certs with openssl so that
+# `docker compose up` works out of the box for every developer and evaluator.
+# ─────────────────────────────────────────────────────────────────────────────
+
+CERT_DIR="/etc/nginx/certs"
+CERT_FILE="$CERT_DIR/selfsigned.pem"
+KEY_FILE="$CERT_DIR/selfsigned-key.pem"
+
+# If mkcert certs are mounted, symlink them to the names nginx.conf expects
+if [ -f "$CERT_DIR/mespapiers.local+1.pem" ] && [ -f "$CERT_DIR/mespapiers.local+1-key.pem" ]; then
+    echo "[nginx-entrypoint] mkcert certificates found — using them (green padlock)"
+    ln -sf "$CERT_DIR/mespapiers.local+1.pem"     "$CERT_FILE"
+    ln -sf "$CERT_DIR/mespapiers.local+1-key.pem"  "$KEY_FILE"
+elif [ ! -f "$CERT_FILE" ] || [ ! -f "$KEY_FILE" ]; then
+    echo "[nginx-entrypoint] No certificates found — generating self-signed certs"
+    apk add --no-cache openssl > /dev/null 2>&1
+    openssl req -x509 -nodes -days 365 \
+        -newkey rsa:2048 \
+        -keyout "$KEY_FILE" \
+        -out "$CERT_FILE" \
+        -subj "/CN=mespapiers.local" \
+        -addext "subjectAltName=DNS:mespapiers.local,DNS:localhost"
+    echo "[nginx-entrypoint] Self-signed certificates generated"
+else
+    echo "[nginx-entrypoint] Self-signed certificates already exist — reusing"
+fi
+
+exec nginx -g "daemon off;"
diff --git a/infra/nginx/nginx.conf b/infra/nginx/nginx.conf
new file mode 100644
index 0000000..46e2e4b
--- /dev/null
+++ b/infra/nginx/nginx.conf
@@ -0,0 +1,69 @@
+# ─────────────────────────────────────────────────────────────────────────────
+# MesPapiers — NGINX reverse proxy
+#
+# Terminates TLS on ports 443 (HTTPS) and redirects port 80 (HTTP) → 443.
+# Proxies all traffic to the Next.js container ("web" service on port 3000).
+# ─────────────────────────────────────────────────────────────────────────────
+
+worker_processes auto;
+
+events {
+    worker_connections 1024;
+}
+
+http {
+    include       /etc/nginx/mime.types;
+    default_type  application/octet-stream;
+
+    sendfile    on;
+    tcp_nopush  on;
+
+    keepalive_timeout  65;
+    client_max_body_size 20M;
+
+    # ── Upstream — the Next.js container ────────────────────────────────────
+
+    upstream nextjs {
+        server web:3000;
+    }
+
+    # ── HTTP → HTTPS redirect ───────────────────────────────────────────────
+
+    server {
+        listen 80;
+        server_name mespapiers.local localhost;
+
+        return 301 https://$host$request_uri;
+    }
+
+    # ── HTTPS server ────────────────────────────────────────────────────────
+
+    server {
+        listen 443 ssl;
+        server_name mespapiers.local localhost;
+
+        # Certs are resolved by entrypoint.sh (mkcert or self-signed)
+        ssl_certificate     /etc/nginx/certs/selfsigned.pem;
+        ssl_certificate_key /etc/nginx/certs/selfsigned-key.pem;
+
+        ssl_protocols TLSv1.2 TLSv1.3;
+        ssl_ciphers HIGH:!aNULL:!MD5;
+        ssl_prefer_server_ciphers on;
+
+        # ── Next.js application ─────────────────────────────────────────────
+
+        location / {
+            proxy_pass http://nextjs;
+
+            proxy_http_version 1.1;
+            proxy_set_header Host              $host;
+            proxy_set_header X-Real-IP         $remote_addr;
+            proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
+            proxy_set_header X-Forwarded-Proto $scheme;
+
+            # WebSocket support (Next.js HMR + future WS engine)
+            proxy_set_header Upgrade    $http_upgrade;
+            proxy_set_header Connection "upgrade";
+        }
+    }
+}
